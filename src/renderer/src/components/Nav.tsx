import { Boxes, Clock3, Settings, TableProperties } from 'lucide-react'
export type PageName='scrape'|'products'|'history'|'settings'
export function Nav({page,onChange}:{page:PageName;onChange:(page:PageName)=>void}){
  const items:Array<{id:PageName;label:string;icon:typeof TableProperties}>=[{id:'scrape',label:'Scrape',icon:TableProperties},{id:'products',label:'Products',icon:Boxes},{id:'history',label:'History',icon:Clock3},{id:'settings',label:'Settings',icon:Settings}]
  return <aside className="flex w-56 shrink-0 flex-col border-r border-slate-200 bg-slate-950 text-white"><div className="border-b border-slate-800 px-5 py-5"><div className="text-sm font-semibold tracking-wide">AVENTICS</div><div className="mt-1 text-xs text-slate-400">Product Scraper v0.2.5</div></div><nav className="p-3">{items.map(({id,label,icon:Icon})=><button key={id} onClick={()=>onChange(id)} className={`mb-1 flex w-full items-center gap-3 rounded-md px-3 py-2.5 text-left text-sm transition ${page===id?'bg-white text-slate-950':'text-slate-300 hover:bg-slate-900 hover:text-white'}`}><Icon size={17}/>{label}</button>)}</nav><div className="mt-auto px-5 py-4 text-[11px] leading-5 text-slate-500">Catalog data · quality · comparison · export</div></aside>
}
